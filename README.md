# Mask the Minion

A MOBA-style 2-player game where heroes collect power masks to buff and transform minions to conquer the enemy base.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   ```
   npm install
   ```
2. Run the app:
   ```
   npm run dev
   ```
